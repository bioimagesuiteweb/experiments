on: [push]
jobs:
  hello_world_job:
    runs-on: macos-10.15
    name: A job to perform regression tests for bisweb
    steps:
    - name: Run
      id: run      
      uses: bioimagesuiteweb/experiments/actions/test@master
      env:
        BIS_FIRST_TEST: 20
        BIS_LAST_TEST: 24
    - name: Results
      run: echo "${{ steps.run.outputs.result}}"
    - name: Logfile
      run: echo "${{ steps.run.outputs.logfile}}"
    