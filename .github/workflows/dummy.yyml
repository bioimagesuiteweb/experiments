on: [push]

jobs:
  hello_world:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ macos-latest, ubuntu-18.04]
    name: search
    steps:
    - name: Begin
      run: |
        export BISWEBOS=`uname`
        echo $BISWEBOS
        cd /
        ls -l
