on: [push]

jobs:
  hosted_build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ macos-latest, ubuntu-18.04]
    name: bisweb-test
    steps:
    - name: Global 
      run: |
        export BISWEBOS=`uname`
        echo $BISWEBOS
        echo "Home"
        ls -R /home
        echo "Users"
        ls -R /Users