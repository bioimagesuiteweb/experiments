on: [push]

jobs:
  hosted_build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-18.04]
    name: bisweb-test
    steps:
    - name: Global 
      run: |
        export BISWEBOS=`uname`
        echo $BISWEBOS
        ls -R /
        echo "Home"
        ls -R /home
